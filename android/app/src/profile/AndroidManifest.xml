<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- ✅ SECURITY: Permissions for profile builds -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <!-- ✅ SECURITY FIX: Explicitly set security settings -->
    <!-- Profile builds (performance testing) don't expose activities externally -->
    <application android:enableOnBackInvokedCallback="true" tools:replace="android:enableOnBackInvokedCallback">
        <meta-data android:name="io.flutter.embedding.android.ImpellerBackend" android:value="opengles" />
        <!-- No activities defined here, inherits from main manifest -->
    </application>
</manifest>
